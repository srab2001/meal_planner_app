# Phase 3 Architecture & Data Flow Diagrams

This document provides visual architecture and data flow diagrams for Phase 3.

---

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                          MEAL PLANNER APP                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                    REACT FRONTEND (Vercel)                │ │
│  │                                                            │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │           Fitness Module (Phase 3 Focus)            │ │ │
│  │  │                                                      │ │ │
│  │  │  ┌────────────────────────────────────────────────┐ │ │ │
│  │  │  │     AIWorkoutInterview.js (REWRITTEN)         │ │ │ │
│  │  │  │     - Fetch admin questions on mount          │ │ │ │
│  │  │  │     - Display questions sequentially           │ │ │ │
│  │  │  │     - Collect structured answers              │ │ │ │
│  │  │  │     - Generate workout with ChatGPT           │ │ │ │
│  │  │  │     - Save to database                        │ │ │ │
│  │  │  └────────────────────────────────────────────────┘ │ │ │
│  │  │                        │                            │ │ │
│  │  │         ┌──────────────┼──────────────┐             │ │ │
│  │  │         │              │              │             │ │ │
│  │  │  ┌──────▼──────┐ ┌──────▼────────┐ ┌──▼──────────┐ │ │ │
│  │  │  │   Text      │ │  Multiple    │ │  Yes/No    │ │ │ │
│  │  │  │  Question   │ │   Choice     │ │  Display   │ │ │ │
│  │  │  │  Display    │ │   Display    │ │            │ │ │ │
│  │  │  │  (NEW)      │ │   (NEW)      │ │  (NEW)     │ │ │ │
│  │  │  └─────────────┘ └──────────────┘ └────────────┘ │ │ │
│  │  │                                                      │ │ │
│  │  │                   ┌──────────────┐                   │ │ │
│  │  │                   │   Range      │                   │ │ │
│  │  │                   │  Display     │                   │ │ │
│  │  │                   │   (NEW)      │                   │ │ │
│  │  │                   └──────────────┘                   │ │ │
│  │  └────────────────────────────────────────────────────┘ │ │ │
│  │                                                            │ │ │
│  │  AIWorkoutInterview.css (EXPANDED - 275 new lines)        │ │ │
│  └──────────────────────────────────────────────────────────┘ │ │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
                             ▲              ▼
                             │              │
                    ┌────────┴──────────────┴────────┐
                    │                                 │
         ┌──────────▼──────────┐    ┌────────────────▼───────┐
         │ GET /api/admin/     │    │ POST /api/fitness/     │
         │ questions/active    │    │ ai-interview           │
         │ (PHASE 1 ENDPOINT)  │    │ (PHASE 1 BACKEND MOD)  │
         └──────────┬──────────┘    └────────────┬───────────┘
                    │                            │
         ┌──────────▼────────────────────────────▼──────────┐
         │                                                  │
         │     EXPRESS.JS BACKEND (Render)                 │
         │     fitness.js Routes                           │
         │                                                  │
         │  POST /api/fitness/ai-interview                 │
         │  - Receive interview_answers                    │
         │  - Updated system prompt                        │
         │  - 6-section workout generation                 │
         │  - JSON parsing & database save                 │
         │  - 3-attempt retry logic                        │
         │                                                  │
         └──────────┬──────────────────────────────────────┘
                    │
         ┌──────────▼──────────┐
         │                     │
    ┌────▼───────┐    ┌───────▼────────┐
    │  OpenAI    │    │  PostgreSQL    │
    │  ChatGPT   │    │  (Render)      │
    │  (API)     │    │                │
    │            │    │ Tables:        │
    │ - Generate │    │ - Users        │
    │   6-sec    │    │ - Workouts     │
    │   workouts │    │ - Questions    │
    │   from     │    │ - Answers      │
    │   answers  │    │                │
    └────────────┘    └────────────────┘
```

---

## Data Flow: Question Phase

```
┌──────────────────────────────────────────────────────────────┐
│           USER OPENS AI WORKOUT COACH                        │
└─────────────────────────────┬────────────────────────────────┘
                              │
                    ┌─────────▼─────────┐
                    │  useEffect Hook   │
                    │  (on mount)       │
                    └─────────┬─────────┘
                              │
                    ┌─────────▼──────────────────┐
                    │ Initialize Interview       │
                    │ setLoading(true)           │
                    └─────────┬──────────────────┘
                              │
                    ┌─────────▼──────────────────────────┐
                    │ Fetch Admin Questions              │
                    │ GET /api/admin/questions/active    │
                    │ Headers: Bearer {token}            │
                    └─────────┬──────────────────────────┘
                              │
                    ┌─────────▼──────────────────────────┐
                    │ Response Received                  │
                    │ Array of questions with:           │
                    │ - id                               │
                    │ - question_text                    │
                    │ - question_type                    │
                    │ - options[] (if applicable)        │
                    │ - order_position                   │
                    └─────────┬──────────────────────────┘
                              │
                    ┌─────────▼──────────────────────────┐
                    │ Questions Exist?                   │
                    └─────────┬──────────────────────────┘
                              │
                ┌─────────────┴─────────────┐
                │                           │
            YES │                           │ NO
                │                           │
       ┌────────▼────────┐        ┌────────▼─────────────┐
       │ Sort by order   │        │ Use default          │
       │ position        │        │ question             │
       │                 │        │                      │
       │ setQuestions([])│        │ setQuestions([{      │
       │ setLoading(false)        │   id: 'default',     │
       └────────┬────────┘        │   question_text:     │
                │                 │   "What type...",    │
                │                 │   question_type: 'text'
                │                 │ }])                  │
                │                 │ setLoading(false)    │
                │                 └────────┬─────────────┘
                │                          │
                └──────────────┬───────────┘
                              │
                    ┌─────────▼──────────────────┐
                    │ Add initial message:       │
                    │ "Hi! I'm your AI Coach..." │
                    │ setMessages([message])     │
                    └─────────┬──────────────────┘
                              │
                    ┌─────────▼──────────────────┐
                    │ READY FOR USER INPUT       │
                    │ Display Question 1         │
                    │ currentQuestionIndex = 0   │
                    └──────────────────────────────┘
```

---

## Data Flow: Answer Collection Phase

```
┌──────────────────────────────────────────────────────────────┐
│        USER ANSWERS CURRENT QUESTION                         │
└─────────────────────────────┬────────────────────────────────┘
                              │
                    ┌─────────▼────────────────┐
                    │ Interacts with component │
                    │ (TextQuestion, MC, etc)  │
                    └─────────┬────────────────┘
                              │
                    ┌─────────▼────────────────────────┐
                    │ Component onAnswer callback      │
                    │ handleAnswerQuestion(answer)     │
                    └─────────┬────────────────────────┘
                              │
                    ┌─────────▼──────────────────────────┐
                    │ Record answer in state:           │
                    │ answers[currentQuestion.id]       │
                    │ = answer value                    │
                    └─────────┬──────────────────────────┘
                              │
                    ┌─────────▼──────────────────────────┐
                    │ Add to message history:           │
                    │ messages = [                      │
                    │   ...,                            │
                    │   { role: 'user',                 │
                    │     content: answer }             │
                    │ ]                                 │
                    └─────────┬──────────────────────────┘
                              │
                    ┌─────────▼──────────────────────────┐
                    │ Check: More questions?             │
                    └─────────┬──────────────────────────┘
                              │
                ┌─────────────┴──────────────┐
                │                            │
            YES │                            │ NO
                │                            │ (all questions answered)
       ┌────────▼────────────┐   ┌──────────▼──────────────┐
       │ Increment            │   │ Call generateWorkout()│
       │ currentQuestionIndex++   │ with all answers      │
       │                     │   │                      │
       │ setCurrentQuestion  │   │ setLoading(true)     │
       │ Index(idx + 1)      │   │                      │
       │                     │   │ Add thinking msg:    │
       │ Re-render with next │   │ "💭 Creating...       │
       │ question via        │   │                      │
       │ renderCurrent       │   │                      │
       │ Question()          │   └──────────┬───────────┘
       │                     │              │
       │ (Loop back to       │              │
       │ display Question N) │              │
       └─────────────────────┘              │
                                            │
                           ┌────────────────┴────────────────┐
                           │ Move to Workout Generation Phase │
                           └─────────────────────────────────┘
```

---

## Data Flow: Workout Generation Phase

```
┌───────────────────────────────────────────────────────────┐
│       GENERATE WORKOUT FROM INTERVIEW ANSWERS             │
└────────────────────────────┬────────────────────────────┘
                             │
                   ┌─────────▼──────────────────┐
                   │ buildAnswersObject()       │
                   │                            │
                   │ For each question:         │
                   │ - Get question ID          │
                   │ - Get user answer          │
                   │ - Create readable key      │
                   │ - Store in interviewAnswers
                   └─────────┬──────────────────┘
                             │
                   ┌─────────▼──────────────────────────┐
                   │ POST /api/fitness/ai-interview     │
                   │                                    │
                   │ Body: {                            │
                   │   interview_answers: {             │
                   │     q1_fitness_goal: "build muscle"│
                   │     q2_experience: "intermediate"  │
                   │     q3_time: 45                    │
                   │     ...                            │
                   │   },                               │
                   │   question_count: 5,               │
                   │   userProfile: {...},              │
                   │   messages: [...]                  │
                   │ }                                  │
                   └─────────┬──────────────────────────┘
                             │
                   ┌─────────▼──────────────────────────┐
                   │ Backend fitness.js                 │
                   │ POST /api/fitness/ai-interview     │
                   │                                    │
                   │ 1. Check interview_answers param   │
                   │ 2. Build context-aware prompt      │
                   │ 3. Call OpenAI ChatGPT             │
                   │ 4. Generate 6-section workout      │
                   └─────────┬──────────────────────────┘
                             │
                   ┌─────────▼──────────────────────────┐
                   │ ChatGPT Response Format:           │
                   │                                    │
                   │ Some intro text...                 │
                   │                                    │
                   │ <WORKOUT_JSON>                     │
                   │ {                                  │
                   │   warm_up: {...},                  │
                   │   strength: {...},                 │
                   │   cardio: {...},                   │
                   │   agility: {...},                  │
                   │   recovery: {...},                 │
                   │   closeout: {...},                 │
                   │   summary: {...}                   │
                   │ }                                  │
                   │ </WORKOUT_JSON>                    │
                   │                                    │
                   │ Some outro text...                 │
                   └─────────┬──────────────────────────┘
                             │
                   ┌─────────▼──────────────────────────┐
                   │ Parse JSON from response           │
                   │ workoutMatch = match               │
                   │ <WORKOUT_JSON>...</WORKOUT_JSON>   │
                   └─────────┬──────────────────────────┘
                             │
                   ┌─────────▼──────────────────────────┐
                   │ JSON.parse(workoutMatch[1])        │
                   │ → workout object                   │
                   └─────────┬──────────────────────────┘
                             │
                   ┌─────────▼──────────────────────────┐
                   │ Save to database                   │
                   │ Attempt 1: fitness_workouts.create │
                   │   If fails → wait 1s               │
                   │   Attempt 2: retry                 │
                   │   If fails → wait 2s               │
                   │   Attempt 3: retry                 │
                   │   If fails → throw error           │
                   └─────────┬──────────────────────────┘
                             │
                   ┌─────────▼──────────────────────────┐
                   │ Return response:                   │
                   │ {                                  │
                   │   message: cleaned_message,        │
                   │   workoutGenerated: true,          │
                   │   workout: workout_object          │
                   │ }                                  │
                   └─────────┬──────────────────────────┘
                             │
                   ┌─────────▼──────────────────────────┐
                   │ Frontend receives response         │
                   │ Parse workout object               │
                   │ setWorkoutGenerated(true)          │
                   │ Add success message                │
                   └─────────┬──────────────────────────┘
                             │
                   ┌─────────▼──────────────────────────┐
                   │ Show 3-second countdown            │
                   │ setTimeout(() => {                 │
                   │   onWorkoutGenerated(workout)      │
                   │ }, 3000)                           │
                   └─────────┬──────────────────────────┘
                             │
                   ┌─────────▼──────────────────────────┐
                   │ INTERVIEW COMPLETE                 │
                   │ Component closes                   │
                   │ Workout ready for Phase 4 display  │
                   └────────────────────────────────────┘
```

---

## Component Communication

```
                    ┌──────────────────────────────────┐
                    │  AIWorkoutInterview (Parent)     │
                    │  - State: questions[], answers{} │
                    │  - Handler: handleAnswerQuestion │
                    │  - Handler: generateWorkout      │
                    └──────────────────────────────────┘
                              ▲  │  │  │  │  ▲
                              │  │  │  │  │  │
                    ┌─────────┴──┴──┴──┴──┴──┴────────┐
                    │                                  │
        Props:      │  question, onAnswer, disabled   │
        onAnswer    │  callback fired on user input   │
                    │                                  │
         ┌──────────▼────────┐  ┌──────────┬─────────┐
         │ TextQuestion      │  │ Multiple │ YesNo   │
         │ Display           │  │ Choice   │ Display │
         │                   │  │ Display  │         │
         │ <textarea>        │  │ <input   │ <button │
         │ "Continue" button │  │ type...> │ onClick │
         └───────────────────┘  └──────────┴─────────┘
                                       │
                                ┌──────▼─────────┐
                                │ RangeDisplay   │
                                │                │
                                │ <input         │
                                │ type="range">  │
                                │ "Continue" btn │
                                └────────────────┘

        All components:
        - Props: { question, onAnswer, disabled }
        - Call: onAnswer(userInput) when user submits
        - Return: JSX rendering question UI
        - Parent handles state/callback
```

---

## Question Type Rendering Logic

```
renderCurrentQuestion() {
  switch(question.question_type) {
    
    case 'text':
    ├─ Render: <TextQuestionDisplay />
    ├─ Input: textarea
    ├─ Submit: Continue button / Ctrl+Enter
    └─ Value: String
    
    case 'multiple_choice':
    ├─ Render: <MultipleChoiceDisplay />
    ├─ Input: radio buttons from question.options
    ├─ Submit: automatic on selection
    └─ Value: Selected option string
    
    case 'yes_no':
    ├─ Render: <YesNoDisplay />
    ├─ Input: Yes/No buttons
    ├─ Submit: automatic on click
    └─ Value: "Yes" or "No" string
    
    case 'range':
    ├─ Render: <RangeDisplay />
    ├─ Input: slider 1-10
    ├─ Submit: Continue button
    └─ Value: Numeric value 1-10
    
    default:
    ├─ Render: <TextQuestionDisplay />
    └─ Fallback for unknown types
  }
}
```

---

## State Management Timeline

```
Component Lifecycle:
├─ MOUNT
│  └─ questions: []
│     currentQuestionIndex: 0
│     answers: {}
│     loading: true
│     workoutGenerated: false
│
├─ INITIALIZE
│  ├─ useEffect hook triggers
│  ├─ Fetch /api/admin/questions/active
│  └─ questions: [q1, q2, q3, ...]
│     loading: false
│
├─ DISPLAY Q1
│  └─ renderCurrentQuestion() shows first question
│     currentQuestionIndex: 0
│
├─ ANSWER Q1
│  └─ handleAnswerQuestion(answer)
│     answers: { q1_id: answer }
│     currentQuestionIndex: 1
│
├─ DISPLAY Q2
│  └─ renderCurrentQuestion() shows next question
│
├─ ANSWER Q2
│  └─ answers: { q1_id: ans1, q2_id: ans2 }
│     currentQuestionIndex: 2
│
├─ ... repeat for all questions ...
│
├─ AFTER LAST ANSWER
│  └─ generateWorkout() called
│     loading: true
│
├─ GENERATING WORKOUT
│  └─ POST /api/fitness/ai-interview
│     with interview_answers
│
├─ WORKOUT RECEIVED
│  └─ workoutGenerated: true
│     loading: false
│
├─ COUNTDOWN
│  └─ setTimeout 3 seconds
│
└─ CLOSE
   └─ onWorkoutGenerated(workout)
      onClose()
      Component unmounts
```

---

## Error Handling Flow

```
            ┌─────────────────────┐
            │   Operation Start   │
            └─────────────┬───────┘
                          │
                    ┌─────▼─────┐
                    │   TRY     │
                    └─────┬─────┘
                          │
            ┌─────────────┴──────────────┐
            │                            │
         FAIL                         SUCCESS
            │                            │
    ┌───────▼────────┐           ┌──────▼───────┐
    │   CATCH Error  │           │   Complete   │
    └───────┬────────┘           └──────────────┘
            │
    ┌───────▼──────────────┐
    │ setError(message)    │
    │ setLoading(false)    │
    └───────┬──────────────┘
            │
    ┌───────▼──────────────┐
    │ Render error UI:     │
    │ - Error message      │
    │ - Close button       │
    │ - User can retry     │
    └──────────────────────┘
```

---

## 6-Section Workout Structure

```
┌────────────────────────────────────────────────────────┐
│                    WORKOUT OBJECT                      │
├────────────────────────────────────────────────────────┤
│                                                         │
│  warm_up                strength              cardio   │
│  ┌──────────┐          ┌──────────┐         ┌────────┐│
│  │ Name     │          │ Name     │         │ Name   ││
│  │ 5 min    │          │ 20 min   │         │ 10 min ││
│  │ - Circles│          │ - Squats │         │ - Run  ││
│  │ - Swings │          │ - Pushups│         │ - Burps││
│  │ - Lunges │          │ - Rows   │         │        ││
│  └──────────┘          │ 3x10 reps│         └────────┘│
│                        └──────────┘                     │
│                                                         │
│  agility                recovery            closeout   │
│  ┌──────────┐          ┌──────────┐         ┌────────┐│
│  │ Name     │          │ Name     │         │ Message││
│  │ 5 min    │          │ 5 min    │         │"Great  ││
│  │ - Ladder │          │ - Ham    │         │work!   ││
│  │ - Cones  │          │ - Quads  │         │350 cal││
│  │ - Touch  │          │ - Back   │         │"       ││
│  └──────────┘          └──────────┘         └────────┘│
│                                                         │
│                    summary                             │
│              ┌────────────────────┐                    │
│              │ Total: 45 minutes  │                    │
│              │ Intensity: High    │                    │
│              │ Calories: 350      │                    │
│              │ Difficulty: 7/10   │                    │
│              └────────────────────┘                    │
│                                                         │
└────────────────────────────────────────────────────────┘
```

---

## API Response Parsing

```
ChatGPT Response String
│
│ "Here's your workout plan..."
│ <WORKOUT_JSON>
│ {...JSON...}
│ </WORKOUT_JSON>
│ "Go crush it!"
│
└─ workoutMatch = regex match
   <WORKOUT_JSON>.....</WORKOUT_JSON>
   │
   └─ JSON.parse(match[1])
      │
      └─ workout object
         │
         └─ Database save
            │
            └─ Return to frontend
               │
               └─ Display (Phase 4)
```

---

## Integration Points

```
                    ┌────────────────────────┐
                    │  Admin Panel (Phase 2) │
                    │  Create questions      │
                    └─────────────┬──────────┘
                                  │
                                  │ Stores in
                                  │
                    ┌─────────────▼──────────┐
                    │ Database Tables        │
                    │ admin_interview_qs     │
                    └─────────────┬──────────┘
                                  │
                                  │ Queries via
                                  │
            ┌─────────────────────▼────────────────┐
            │ Backend Endpoint                    │
            │ GET /api/admin/questions/active    │
            └─────────────────────┬────────────────┘
                                  │
                                  │ Called by
                                  │
            ┌─────────────────────▼────────────────┐
            │ AI Coach (Phase 3) - NEW             │
            │ AIWorkoutInterview.js                │
            └─────────────────────┬────────────────┘
                                  │
                                  │ Generates workout via
                                  │
            ┌─────────────────────▼────────────────┐
            │ Backend Endpoint                    │
            │ POST /api/fitness/ai-interview      │
            └─────────────────────┬────────────────┘
                                  │
                                  │ Saves to database
                                  │
            ┌─────────────────────▼────────────────┐
            │ Workout Display (Phase 4) - PENDING  │
            │ Shows 6-section workout              │
            └──────────────────────────────────────┘
```

---

## Conclusion

These diagrams show:
- System architecture and component relationships
- Data flow through the interview process
- State management timeline
- Error handling flows
- Component communication patterns
- Integration with existing system

Phase 3 successfully connects Phase 1-2 (admin configuration) with Phase 4 (workout display) through the rewritten AI Coach interview system.
