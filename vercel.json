{
  "buildCommand": "cd client && npm ci && npm run build",
  "outputDirectory": "client/build",
  "framework": "react",
  "rootDirectory": ".",
  "ignoreFiles": [
    "node_modules/**",
    ".git/**",
    ".vercel/**"
  ]
}
